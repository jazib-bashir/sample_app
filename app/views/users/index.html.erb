<% provide(:title, 'All users') %>
<h1>All users</h1>
<div class="row">
   <div class="span6 offset3">
 
<%= form_tag users_path, method: :get do  %>
 <%= text_field_tag :query, params[:query], placeholder: "Plz enter your friend name here to find"%>
 <%= submit_tag "Search", name: nil, class: "btn btn-small btn-primary"%>
<% end %>
</div>
</div>
<%= will_paginate %>






<ul class="users" >
  <%= render @users %>
</ul>

<%= will_paginate %>